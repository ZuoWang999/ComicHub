# ComicHub 漫画平台 - 会话记录

## 项目概述
React + Node.js + MongoDB 全栈漫画平台，支持在线阅读、用户系统、讨论社区、后台管理。

## 技术栈
| 层 | 技术 |
|---|---|
| 前端 | React 18 + Vite + Tailwind CSS + React Router + Axios |
| 后端 | Node.js + Express + Mongoose |
| 数据库 | MongoDB |
| 认证 | JWT (jsonwebtoken + bcrypt) |
| 文件存储 | 本地磁盘 (multer) |

## 项目路径
`D:\AI\OpenCode\DK\`

## 启动方式
```bash
# 终端1 - 后端
cd D:\AI\OpenCode\DK\server
node index.js

# 终端2 - 前端
cd D:\AI\OpenCode\DK\client
npm run dev
```
浏览器打开 `http://localhost:3000`

## 功能模块
- 首页 — 轮播推荐 + 热门漫画 + 最新更新 + 热门讨论
- 浏览 — 分类/状态筛选 + 搜索 + 排序 + 分页
- 漫画详情 — 封面、简介、章节列表、评论、收藏、关联社区帖
- 在线阅读器 — 滚动/单页双模式、章节导航、键盘翻页
- 用户系统 — 注册/登录、个人中心、收藏、发帖记录
- 讨论社区 — 版块分类、发帖/回帖、点赞、关联漫画
- 后台管理 — 漫画CRUD、章节上传(多图)、版块管理

## 首次使用
1. 在网站注册一个账号
2. 在 MongoDB 中把该用户的 `role` 从 `user` 改为 `admin`
3. 登录后就拥有管理员权限，可访问 `/admin` 后台

---

保存时间: 2026-05-01
